version: 1.0
runtime: python311

build:
  commands:
    build:
      - pip3 install --upgrade pip
      - pip3 install flask==2.3.2

run:
  runtime-version: 3.11
  command: python3 app.py
  network:
    port: 8000
